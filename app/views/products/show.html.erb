

<div class="banner background-div" style="background: url('<%= cl_image_path(@product.photo.key) %>') no-repeat center center fixed; background-size: cover; object-position: center;">
  <div class="container">
    <h1><%= @product.name %></h1>
    <p class="color-white mt-2 fs-6">STARTING FROM <%= @product.price %>$</p>
  </div>
</div>

<div class="container w-75 mx-auto mt-5 p-5 shadow-lg rounded bg-white">
      <p class="h5 lh-base"><%= @product.overview %></p>
  </div>
</div>
<%= render 'bookings/booking_form' %>
